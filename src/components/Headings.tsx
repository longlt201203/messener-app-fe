import styled from "styled-components";

const h1 = styled.h1`
    font-size: 2rem;
    font-weight: bold;
`

const h5 = styled.h5`
    font-size: 1rem;
    font-weight: bold;
`

const Headings = {
    h1,
    h5
};

export default Headings;